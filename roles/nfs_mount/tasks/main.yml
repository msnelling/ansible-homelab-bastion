---
- name: ensure mount point exists
  file:
    path: "{{ nfs_mount_path }}"
    state: directory

- name: create mount
  mount:
    path: "{{ nfs_mount_path }}"
    src: 192.168.1.2:/Download
    fstype: nfs
    opts: rsize=8192,wsize=8192,timeo=14,intr
    state: present
